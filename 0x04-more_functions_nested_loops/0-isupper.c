#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: characters for testing
 * Return: 1 if c is upper 0 if c is lower
 */
int _isupper(int c)
{
	if ((c >= 'A') && ((c <= 'Z'))
			{
			return (1);
			}

			return (0);
}
